A mini social network project built in C to manage and monitor user profiles and their follow relationships using dynamic linked lists, where the system automatically registers new accounts upon interaction, tracks follow and unfollow actions instantly, updates follower and unfollower metrics for each account with high accuracy, and prevents data duplication within memory by updating the follow status rather than performing random deletions to ensure the stability of every user's statistical record.
